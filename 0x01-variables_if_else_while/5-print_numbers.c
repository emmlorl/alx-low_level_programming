#include <stdio>

/**
 * main - printing all single digits of base 10 from 0,
 * followed by new line
 * Return: Always 0 (Success)
 */
int main(void)
{
	int n = 0;
	int i;

	while (n < i)
	{
		printf("%d", n);
	n++;
	}
	printf("\n");
	return (0);
}
